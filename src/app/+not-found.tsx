import { View } from 'react-native';
import PageNotFound from '~src/components/error/pageNotFound.component';

export default () => (
  <View className='flex-1 bg-background'>
    <PageNotFound />
  </View>
);
